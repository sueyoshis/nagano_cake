<h2>注文履歴詳細</h2>

<table>
  <tr>
    <th>購入者</th>
    <td>
      <%= link_to admin_customer_path(@order.customer) do%>
        <%= @order.customer.full_name %>
      <% end %>
    </td>
  </tr>
  <tr>
    <th>注文日</th>
    <td>
      <%= @order.created_at.strftime("%Y/%m/%d") %>
    </td>
  </tr>
  <tr>
    <th>配送先</th>
    <td>
      〒<%= @order.postal_code %> <%= @order.address %><br>
      <%= @order.name %>
    </td>
  </tr>
  <tr>
    <th>支払方法</th>
    <td>
      <%= @order.payment_method_i18n %>
    </td>
  </tr>
  <tr>
    <th>注文ステータス</th>
    <td>
      入金待ち　更新
    </td>
  </tr>
</table>

<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
      <th>製作ステータス</th>
    </tr>
  </thead>
  <tbody>
    <% @order_items.each do |order_item| %>
      <tr>
        <td>
          <%= order_item.item.name %>
        </td>
        <td>
          <%= order_item.with_tax_price.to_s(:delimited) %>
        </td>
        <td>
          <%= order_item.amount %>
        </td>
        <td>
          <%= order_item.subtotal.to_s(:delimited) %>
        </td>
        <td>
          着手不可　更新
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<table>
  <tr>
    <th>商品合計</th>
    <td>
      <%= @order.total_price.to_s(:currency, format: "%n%u", unit: "円", precision: 0) %>
    </td>
  </tr>
  <tr>
    <th>送料</th>
    <td>
      <%= @order.postage.to_s(:currency, format: "%n%u", unit: "円", precision: 0) %>
    </td>
  </tr>
  <tr>
    <th>請求金額合計</th>
    <td><%= @order.billing_amount.to_s(:currency, format: "%n%u", unit: "円", precision: 0) %></td>
  </tr>
</table>