<% items.each do |item| %>
  <%= link_to item_path(item.id) do %>
  <%= attachment_image_tag item, :image, format: 'jpeg', size: '100x100', fallback: "no_image.jpg" %>
  <%= item.name %>
  <%= item.with_tax_price.to_s(:currency, unit: "¥", precision: 0 ) %><span>(税込)</span>
  <% end %>
<% end %>
